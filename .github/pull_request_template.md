# What this changes

<!-- The behaviour change, in a sentence or two. Why, not a file list. -->

## How it was validated

<!-- Commands run and what they showed. Example: `make all`, and `make run` for anything the window or menu bar does. -->

## Risk

<!-- What could this break? Anything that replaces an app, touches the trust gate,
signing, or the release pipeline deserves a line here. -->

## Related

<!-- Closes #N / relates to #N. -->

- [ ] `CHANGELOG.md` updated under *Unreleased* for a user-visible change
- [ ] `make generate` run and `OpenFreshr.xcodeproj` committed if `project.yml` changed
